export const metadata = {
  title: 'Greenvelly | SiteManage X',
  description: 'Greenvelly site management for builders and admins.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function GreenvellyLayout({ children }) {
  return children;
}
